# 基于实际专家知识的增强型知识注入系统 — 丁华秘籍合并说明

摘要文件: `docs/丁华掼蛋秘籍_整合.md`（来源：`docs/skill/掼蛋技巧秘籍_OCR.txt`）。

建议合并点：
- 在 `RealExpertKnowledgeExtractor` 的 `extraction_patterns` 中增加或校准与丁华秘籍对应的 pattern/keywords，优先提高 `TACTICAL_COOPERATION`、`UPGRADE_STRATEGY` 与 `TIMING_MASTERY` 的置信度阈值。
- 把 `ExpertKnowledgeFormatter` 的 `category_to_priority` 参考丁华摘要再微调，并把部分高价值规则设为自动高优先级（priority >= 0.9）。

我可以自动化完成：
- 运行抽取器并生成标准化知识项（JSON），并将高置信度项写入注入系统；
- 或仅生成建议的 pattern/keywords 更新补丁供人工审阅。
